# Copy to .env and fill in. NEVER commit .env (it is already in .gitignore).

# OpenAI API key
OPENAI_API_KEY=

# Chat model
OPENAI_MODEL=gpt-4o-mini

# News provider (OPTIONAL secondary; the primary source is DuckDuckGo, no key needed)
TAVILY_API_KEY=
